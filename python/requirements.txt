Django==4.2.27            # older Django with many fixes after this release
urllib3==2.6.0          # older urllib3 with CVE history
PyYAML==3.13             # older PyYAML with issues (use safe_load recommended)
requests==2.19.1         # older requests version
